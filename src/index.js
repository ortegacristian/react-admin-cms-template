// React
import React from 'react';
import ReactDOM from 'react-dom';

// Estilos
import './index.scss';

// Componentes
import { App } from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root'),
);
